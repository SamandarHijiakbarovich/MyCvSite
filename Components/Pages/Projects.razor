@page "/projects"
@using MyCvSite.Data

<PageTitle>Loyihalarim | Samandar Mamasoatov</PageTitle>

<div class="projects-page">
    <div class="container py-5">
        <div class="section-header text-center mb-5">
            <div class="page-badge mb-3">
                <i class="fas fa-code-branch me-2"></i> PORTFOLIO
            </div>
            <h1 class="display-4 fw-bold mb-3 highlight-text">Mening Loyihalarim</h1>
            <p class="lead custom-subtitle">
                .NET ekotizimida yaratilgan real va ishlab turgan loyihalar
            </p>
            <div class="header-line"></div>
        </div>

        <div class="row g-4">
            @foreach (var project in projects)
            {
                <div class="col-lg-4 col-md-6 animate__animated animate__fadeInUp">
                    <div class="project-card">
                        <div class="project-image">
                            <img src="@project.ImageUrl" alt="@project.Title"
                                 onerror="this.src='https://placehold.co/400x250/1e293b/6366f1?text=@Uri.EscapeDataString(project.Title)'" />
                            <div class="project-overlay">
                                <div class="project-links">
                                    @if (!string.IsNullOrEmpty(project.GithubUrl))
                                    {
                                        <a href="@project.GithubUrl" target="_blank" rel="noopener"
                                           class="project-link" title="GitHub">
                                            <i class="fab fa-github"></i>
                                        </a>
                                    }
                                    @if (!string.IsNullOrEmpty(project.LiveDemoUrl))
                                    {
                                        <a href="@project.LiveDemoUrl" target="_blank" rel="noopener"
                                           class="project-link" title="Live Demo">
                                            <i class="fas fa-external-link-alt"></i>
                                        </a>
                                    }
                                </div>
                            </div>
                        </div>

                        <div class="project-content">
                            <h4 class="project-title">@project.Title</h4>
                            <p class="project-description">@project.Description</p>

                            <div class="tech-tags">
                                @foreach (var tech in project.Technologies)
                                {
                                    <span class="tech-tag">@tech</span>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<style>
    :root {
        --bg-dark: #0f172a;
        --card-bg: #1e293b;
        --accent-blue: #3b82f6;
        --accent-purple: #8b5cf6;
        --text-main: #f8fafc;
        --text-muted: #94a3b8;
        --border-color: rgba(255, 255, 255, 0.1);
    }

    .projects-page {
        background-color: var(--bg-dark);
        min-height: 100vh;
        color: var(--text-main);
    }

    .page-badge {
        display: inline-flex;
        align-items: center;
        background: rgba(99, 102, 241, 0.1);
        border: 1px solid rgba(99, 102, 241, 0.25);
        color: #818cf8;
        padding: 5px 16px;
        border-radius: 50px;
        font-size: 0.8rem;
        font-weight: 700;
        letter-spacing: 1.5px;
    }

    .highlight-text {
        background: linear-gradient(90deg, #3b82f6, #8b5cf6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 3rem;
    }

    .custom-subtitle {
        color: #cbd5e1;
        font-weight: 400;
        max-width: 600px;
        margin: 0 auto;
    }

    .header-line {
        width: 80px;
        height: 4px;
        background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple));
        margin: 20px auto;
        border-radius: 2px;
        box-shadow: 0 0 10px rgba(59, 130, 246, 0.4);
    }

    /* Card Design */
    .project-card {
        background: var(--card-bg);
        border-radius: 20px;
        overflow: hidden;
        border: 1px solid var(--border-color);
        height: 100%;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    }

    .project-card:hover {
        transform: translateY(-12px);
        border-color: var(--accent-blue);
        box-shadow: 0 20px 40px rgba(59, 130, 246, 0.2);
    }

    /* Image section */
    .project-image {
        position: relative;
        height: 220px;
        background: #0b1120;
    }

    .project-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0.85;
        transition: 0.5s;
    }

    .project-card:hover .project-image img {
        opacity: 0.35;
        transform: scale(1.05);
    }

    /* Overlay */
    .project-overlay {
        position: absolute;
        inset: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        background: rgba(15, 23, 42, 0.5);
        opacity: 0;
        transition: 0.3s;
    }

    .project-card:hover .project-overlay {
        opacity: 1;
    }

    .project-link {
        width: 50px;
        height: 50px;
        background: var(--accent-blue);
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
        text-decoration: none;
        transition: 0.3s;
    }

    .project-link:hover {
        background: white;
        color: var(--accent-blue);
        transform: scale(1.2);
    }

    /* Card Content */
    .project-content {
        padding: 1.5rem;
    }

    .project-title {
        color: #fff;
        font-size: 1.3rem;
        font-weight: 700;
        margin-bottom: 0.7rem;
    }

    .project-description {
        color: #94a3b8;
        font-size: 0.92rem;
        line-height: 1.65;
        margin-bottom: 1.2rem;
        min-height: 4.8rem;
    }

    /* Tech tags */
    .tech-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 7px;
    }

    .tech-tag {
        background: rgba(59, 130, 246, 0.1);
        color: #60a5fa;
        padding: 4px 11px;
        border-radius: 7px;
        font-size: 0.72rem;
        font-weight: 600;
        border: 1px solid rgba(59, 130, 246, 0.2);
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    @@media (max-width: 768px) {
        .highlight-text { font-size: 2.2rem; }
        .project-image { height: 180px; }
    }
</style>

@code {
    private List<ProjectModel> projects = new();

    protected override void OnInitialized()
    {
        projects = new List<ProjectModel>
        {
            new ProjectModel
            {
                Id = 1,
                Title = "EduProctor",
                Description = "AI asosida ishlovchi onlayn imtihon nazorat tizimi. Real vaqtda yuz tanish, xatti-harakat tahlili va SignalR orqali monitoring.",
                ImageUrl = "images/OIP.webp",
                Technologies = new[] { "ASP.NET Core", "SignalR", "EF Core", "PostgreSQL", "JWT", "AI/ML" },
                GithubUrl = "https://github.com/SamandarHijiakbarovich",
                LiveDemoUrl = ""
            },
            new ProjectModel
            {
                Id = 2,
                Title = "LiderBooks",
                Description = "Kitob savdosi uchun to'liq funksional e-commerce platformasi. Mahsulot katalogi, savat tizimi va to'lov integratsiyasi.",
                ImageUrl = "images/API.webp",
                Technologies = new[] { "ASP.NET Core", "EF Core", "SQL Server", "Redis", "Clean Architecture", "LINQ" },
                GithubUrl = "https://github.com/SamandarHijiakbarovich",
                LiveDemoUrl = ""
            },
            new ProjectModel
            {
                Id = 3,
                Title = "Darmon Dorixona",
                Description = "Dorixona uchun raqamli to'lov va buyurtma tizimi. Click/Payme integratsiyasi, rol asosidagi kirish boshqaruvi.",
                ImageUrl = "images/Darmon.jpg",
                Technologies = new[] { "ASP.NET Core", "EF Core", "PostgreSQL", "Click API", "JWT", "xUnit" },
                GithubUrl = "https://github.com/SamandarHijiakbarovich/Darmon",
                LiveDemoUrl = ""
            }
        };
    }
}
